/*
 * This program writes 0 to 999 to console.
 * Can be used to test VM's output performance.
 */

int main()
{
    int i = 0;
    while (i < 1000)
    {
        write(i);
        i = i + 1;
    }
    return 0;
}